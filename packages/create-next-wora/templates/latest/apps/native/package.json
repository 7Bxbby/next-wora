{
  "name": "next-wora-native",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "android:init": "npx cap add android && node scripts/android-init.js",
    "sync": "cap sync",
    "android": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.6.0"
  }
}
